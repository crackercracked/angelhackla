const config = {
    paths: {
        src: {
            js: 'src/**/*.js',
        },
        dist: {
            js: 'dist/**/*.js',
            dir: 'dist',
        }
    }
};

export default config;